name: Spring CI/CD Windows Deployment

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: self-hosted

    env:
      DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      DB_URL: ${{ secrets.DB_URL }}
      DB_USER: ${{ secrets.DB_USER }}
      SERVER_PORT: ${{ secrets.SERVER_PORT }}

    steps:
      - name: 🛡️ Git Global Safe Directory Config
        run: git config --global --add safe.directory C:/actions-runner/_work/roadmate-backend/roadmate-backend
        shell: powershell

      # 🔔 기존 체크아웃 스텝
      - name: 🔔 코드 체크아웃 (Checkout Code)
        uses: actions/checkout@v4

      - name: ☕ JDK 21 환경 설정
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: 📦 Gradle 빌드 실행
        # 빌드 결과물은 Runner Workspace 내의 .\build\libs 에 생성됩니다.
        run: .\gradlew clean bootJar

      - name: 🚀 배포 스크립트 실행 (Windows Batch)
        # 🚨 경로 수정: 러너의 CWD(Git Repo Root)에서 상대 경로로 실행
        shell: cmd
        run: |
          .\deploy.bat