spring:
  application:
    name: roadmate-backend

  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update # 엔티티 추가로 인해 'update' 유지
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: false
        use_sql_comments: true
    open-in-view: false

weather:
  api:
    key: ${WEATHER_API_KEY}
    base-url: http://apis.data.go.kr/1360000/VilageFcstInfoService_2.0

# 💡 수정/추가: 보행 네트워크 API 설정을 다시 추가합니다.
seoul:
  api:
    key: ${WALK_API_KEY} # 환경 변수 유지
    base-url: http://openapi.seoul.go.kr:8088
    service-name: TbTraficWlkNet
    page-size: 1000

server:
  port: ${SERVER_PORT:8080}
  shutdown: graceful
  error:
    include-message: always
    include-stacktrace: on_param

# 🔑 JWT 설정
jwt:
  secret: ${JWT_SECRET_KEY:MySuperSecretKeyForJWTGeneration123456789012345ABCDEF}
  access:
    expiration: ${JWT_ACCESS_EXPIRATION:1800000}
  refresh:
    expiration: ${JWT_REFRESH_EXPIRATION:1209600000}

# Spring Boot Actuator (헬스체크 필수!)
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
      base-path: /actuator
  endpoint:
    health:
      show-details: when-authorized
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true

# 로깅 설정
logging:
  level:
    root: INFO
    com.yourpackage: DEBUG
    # 💡 SQL 로그 설정 유지
    org.hibernate.SQL: OFF
    org.hibernate.type.descriptor.sql.BasicBinder: OFF
    reactor.netty.http.client: DEBUG
    org.springframework.web.reactive.function.client: DEBUG
  file:
    name: ${user.home}/roadmate-backend/logs/application.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# 📖 SpringDoc OpenAPI/Swagger UI 설정 (⭐ 이 부분을 logging 아래에 위치시켰습니다)
springdoc:
  # 🔑 오류 해결 핵심 설정: Generic Response 자동 분석 비활성화
  override-with-generic-response: false

  swagger-ui:
    # UI 접속 경로 지정
    path: /swagger-ui.html
    # API 문서 접속 시도 시 엔드포인트 자동 정렬
    display-operation-id: true
    # 문서 엔드포인트 리스트 정렬 기준 (alpha, method, none)
    operations-sorter: alpha

  # OpenAPI Info 설정 (문서 제목, 설명, 버전)
  info:
    title: ${spring.application.name} API Documentation 🚗
    description: "RoadMate 백엔드 서비스의 모든 API 명세입니다."
    version: "v1.0.0"
    terms-of-service: "http://example.com/terms/"
    contact:
      name: "개발팀"
      url: "http://example.com/contact"
      email: "dev@example.com"
    license:
      name: "Apache 2.0"
      url: "http://www.apache.org/licenses/LICENSE-2.0.html"